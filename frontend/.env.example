# AlertIQ Frontend Environment Variables

# Base URL for the AlertIQ Backend API
# In local development using the Vite dev proxy, this can remain empty (defaults to '/api')
# In production or standalone deployments, set this to the backend host (e.g. http://localhost:5000)
VITE_API_BASE_URL=
